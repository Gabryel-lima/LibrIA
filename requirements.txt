# LibrIA - Reconhecimento de Libras
# Core Dependencies - Compatible with Python 3.11+
# Production-ready with GPU (CUDA) support

# ⚠️  NOTE: TensorFlow and PyTorch require AVX CPU support
# If you get "illegal hardware instruction" errors, use these packages instead:
# - JAX with CPU backend: jax[cpu]
# - Or use a machine with AVX support

# Core ML/DL (COMMENTED OUT IF - requires AVX)
tensorflow==2.16.1
keras==3.13.2
torch==2.8.0
torchvision==0.20.1

# Landmark Detection (if using MediaPipe)
mediapipe==0.10.14  # ⚠️ Requires AVX support

# Pure Python ML (works on any CPU)
scikit-learn==1.6.1

# Vision & Media
opencv-python==4.11.0.86
opencv-contrib-python==4.11.0.86
# mediapipe==0.10.14  # ⚠️ Requires AVX support - use on machine with AVX
Pillow==12.1.1

# Numerical & Scientific
numpy==1.26.4
scipy==1.15.1
pandas==2.2.3
seaborn==0.13.2

# Audio (for gesture recognition support)
sounddevice==0.5.2

# Utilities
joblib==1.4.2
python-dotenv==1.0.1
tqdm==4.67.1
pybind11==2.13.6

# Visualization & Logging
matplotlib==3.10.0
# tensorboard==2.16.1
kagglehub==0.3.6
